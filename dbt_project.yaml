name: 'Analytics'
version: '1.0'
config-version: 2

profile: 'Databricks'

model-paths: ["models"]

models:
  databricks_project:
    staging:
      +materialized: view
    marts:
      +materialized: table
